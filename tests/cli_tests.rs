use assert_cmd::Command;
use std::fs;
use tempfile::tempdir;

// ... (previous tests unchanged, omitted for brevity – we'll include only the changed test)
// To keep the script short, we'll replace just the specific test using sed.

