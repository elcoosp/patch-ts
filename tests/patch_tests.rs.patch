--- a/tests/patch_tests.rs
+++ b/tests/patch_tests.rs
@@ -185,13 +185,16 @@
 #[test]
 fn test_apply_unified_diff_malformed() {
     let dir = tempdir().unwrap();
     let file_path = dir.path().join("sample.rs");
-    fs::write(&file_path, "line1\nline2\n").unwrap();
+    fs::write(&file_path, "completely different content\n").unwrap();

-    let diff = "this is not a valid unified diff";
+    // A valid diff that expects a line that doesn't exist in the file
+    let diff = r#"--- a/sample.rs
++++ b/sample.rs
+@@ -1,3 +1,3 @@
+ line1
+-line2
++new line2
+ line3
+"#;
     let options = PatchOptions::default();
     let result = apply_unified_diff(&file_path, diff, options);
     assert!(result.is_err());
