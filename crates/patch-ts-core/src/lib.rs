pub use patch_ts::{
    ast, patch, matching, repair, semdiff, file, history, compile, indent,
    identifier, provenance, scitt, symbols, validate, crossfile, knowledge,
    sanitize, extract, fix, remote, marker, invariant, coverage, audit,
    curiosity, plugin, plugin_bindings, diagnostics, config, highlight,
    word_diff, scorecard, render_md, fs, lsp_client, recall, session, heal,
};
