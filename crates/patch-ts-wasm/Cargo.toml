[package]
name = "patch-ts-wasm"
version = "1.17.0"
edition = "2021"
description = "WASM bindings"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
patch-ts = { path = "../.." }
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
